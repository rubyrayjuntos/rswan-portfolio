@charset "UTF-8";

/* ParallaxThemes.html specific styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Accessibility: prefers-reduced-motion support */
@media (prefers-reduced-motion: reduce) {
    .hero-bg, .hero-particles, .gallery-item, .timeline-item, .particle {
        transform: none !important;
        transition: none !important;
        animation: none !important;
    }
    
    .spec-card, .spec-card .floating-icon, .spec-card .particle-effect {
        transform: none !important;
        transition: none !important;
        animation: none !important;
    }
    
    .spec-card:hover {
        transform: none !important;
    }
    
    .particle, .journey-particle {
        display: none !important;
    }
    
    .hero-bg, .journey-hero-bg {
        background: linear-gradient(135deg, #051772 0%, #06000c 100%) !important;
        animation: none !important;
    }
    
    .gallery-item:hover {
        transform: none !important;
    }
    
    .timeline-item.visible {
        opacity: 1 !important;
        transform: none !important;
    }
    
    .journey-hero-bg, .journey-particles {
        opacity: 0.3 !important;
    }
}

/* Motion disabled class for user toggle */
.motion-disabled .hero-bg,
.motion-disabled .hero-particles,
.motion-disabled .gallery-item,
.motion-disabled .timeline-item,
.motion-disabled .particle {
    transform: none !important;
    transition: none !important;
    animation: none !important;
}

/* [Rest of the styles from the style element in ParallaxThemes.html] */
/* Note: Copy all remaining styles from the original style block */

/* Content Section Display States */
.content-section.fade-in {
    display: none;
}

/* Modal States */
.gallery-modal-overlay,
.gallery-modal-grid,
.gallery-modal-img-container,
.markdown-modal-overlay,
#asteroids-game-modal {
    display: none;
}

/* Asteroids Game Modal Styles */
.asteroids-modal-footer {
    text-align: center;
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.9rem;
    margin-top: 15px;
}

.asteroids-controls-info {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
}

/* Mobile responsiveness and keyframe animations remain in this file */
@media (max-width: 768px) {
    /* [Mobile-specific styles from original file] */
}

/* All keyframe animations */
@keyframes floatBackground {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-30px) rotate(180deg); }
}

@keyframes particleFloat {
    /* [Animation keyframes from original file] */
}

/* [Other keyframe animations from original file] */
